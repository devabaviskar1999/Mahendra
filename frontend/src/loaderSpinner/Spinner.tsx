import { Loader } from "lucide-react";
const Spinner = () => {
    return (
        <div>
            <Loader size={65} className="animate-spin mb-20" />
            
        </div>
    );
}

export default Spinner;
