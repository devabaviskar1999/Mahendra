import { Loader } from "lucide-react";
const Spinner = () => {
    return (
        <div>
            <Loader size={65} className="animate-spin" />
            
        </div>
    );
}

export default Spinner;
